# Graph DB Benchmark

Benchmark harness for comparing CognoDB Cloud against managed graph database platforms.

Run the benchmark, generate charts, then render this README with:

```bash
bash scripts/run_all.sh
python -m harness.generate_readme
```
